int main(){
    decl 
        int fd,temp,permission,num;
        string fileName,word;
    enddecl

    begin
        fileName = "one.dat";        
        fd = exposcall("Open",fileName);

        if(fd < 0) then
            temp = exposcall("Write",-2,"error");
        else
            temp = exposcall("Seek",fd,1100);
            num = 2000;
            while(num <= 2513) do
                temp = exposcall("Write",fd,num);
                num = num + 1;
            endwhile;
            

            while(1 == 1) do
                temp = exposcall("Seek",fd,-1);
                if(temp == -2) then
                    break;
                endif;

                temp = exposcall("Read",fd,word);
                if(temp == -2) then
                    break;
                endif;
                temp = exposcall("Write", -2, word);
                temp = exposcall("Seek",fd,-1);
            endwhile;   

        endif;
        
        return 0;
    end
}